# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local AI guidance (not committed)
CLAUDE.md

# generated wasm/web build artifacts (regenerated by scripts/build-*.sh):
# object caches (build/wasm-obj, build/web-obj), the wasm smoke output
# (build/wasm), the browser engine module (public/wasm/*.{js,wasm,data}), and
# the PPEF font copies served to the web app (public/resources/).
/build/
/public/wasm/
/public/resources/
# Auto Punch tracer wasm (scripts/build-tracer.sh — quantette + visioncortex)
/public/tracer/
# design library payload (scripts/build-library.sh — manifests + design files
# + baked thumbs from the legacy asset trees; prod serves it from the CDN)
/public/library/
